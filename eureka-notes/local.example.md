<!-- Template only. Copy to local/notes.md, replace placeholders, and remove
unused sections and this comment before pasting into Eureka. Never store secrets. -->

## Git Conventions (Local)

- Branch names should start with `users/<username>/YYMM-`.

## Tooling (Local)

- On this machine, `<tooling-preference-that-differs-from-the-common-notes>`.

## Project Aliases

Use these aliases when I mention another repo by name:

- `<project-alias>`: `<absolute-path-to-repo-on-this-machine>`

## Repo-bundled Skills (Local)

- For `<project-alias>`, read skills from `<skills-directory-relative-to-that-repo>`.
- `<skill-slug>`: `<when-to-use-this-skill>`.
